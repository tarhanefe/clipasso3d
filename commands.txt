To get GPU from the cluster: 
srun --gres=gpu:2 --cpus-per-task=4 --mem=16G --time=12:00:00 --pty bash

To reach different directories:
cd /home/tarhan
cd /scratch/tarhan


For the envorinment:
# 1. Create a conda environment
conda create -f environment.yaml
conda activate clipasso3d


# 2. Install the required packages
pip install torch==2.4.1+cu124 torchvision==0.19.1+cu124 --extra-index-url https://download.pytorch.org/whl/cu124
pip install ninja
pip install git+https://github.com/NVlabs/tiny-cuda-nn/#subdirectory=bindings/torch
pip install nerfstudio

#For using blender renders:
pip install opencv-python



# 3. A different trial 

conda create -n clipasso3d python=3.9
conda activate clipasso3d

pip install torch==2.1.2+cu118 torchvision==0.16.2+cu118 --extra-index-url https://download.pytorch.org/whl/cu118
pip install numpy==1.26.4
conda install -c "nvidia/label/cuda-11.8.0" cuda-toolkit

echo 'export LD_LIBRARY_PATH=/usr/local/cuda-12.1/compat/lib.real:$LD_LIBRARY_PATH' >> ~/.bashrc
source ~/.bashrc

echo 'export TCNN_CUDA_ARCHITECTURES=80' >> ~/.bashrc
source ~/.bashrc

sudo ln -s /usr/lib/x86_64-linux-gnu/libcuda.so.1 /usr/lib/x86_64-linux-gnu/libcuda.so

export LD_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu:$LD_LIBRARY_PATH

pip install ninja git+https://github.com/NVlabs/tiny-cuda-nn/#subdirectory=bindings/torch

pip install nerfstudio 
pip install gsplat==0.1.6
pip install huggingface_hub==0.21.0
pip install tyro==0.6.6


ns-train nerfacto --pipeline.model.use-appearance-embedding False blender-data --data data/chair 